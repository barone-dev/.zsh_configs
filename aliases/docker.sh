# alias d_system_clean_up="docker system prune -a"
# alias d_system_space_usage="docker system df"

# alias d_images_ids="docker images -a -q"
# alias d_images_delete="docker rmi -f $(docker images -a -q)"